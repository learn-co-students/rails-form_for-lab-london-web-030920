<h1>School Classes</h1>
<% @school_classes.each do |school_class| %>
    <div><%= link_to school_class.title + ", Room " + school_class.room_number.to_s, school_class_path(school_class) %> </div>
  <% end %>